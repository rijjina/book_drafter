# Teaching Material Revision Approval

```yaml
schema_version: "1.0"
task: "teaching-material-revision"
status: "CHANGES_REQUESTED"
assessment_id: "{{assessment-id}}"
assessment_package: "{{absolute-package-path}}"
input_path: "{{absolute-input-path}}"
input_sha256: "{{full-sha256}}"
approved_revision_ids: "{{RV-001,RV-002}}"
approved_at: "{{YYYY-MM-DD}}"
approved_by: "{{author-or-authorized-reviewer}}"
```

Approval applies only to the listed revision IDs. Any input, rule, or assessment
change requires a new assessment and approval record.

